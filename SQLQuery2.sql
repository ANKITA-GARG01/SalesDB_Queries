-- CODING ORDER
/*SELECT DISTINCT TOP 2
COL1,
COL2
FROM TABLE
WHERE COL=10
GROUP BY COL1
HAVING SUM(COUNT2)>30
ORDER BY COL1 DESC*/  


--STATIC(FIXED ) SQL QUERY
SELECT 
id,
first_name,
'New Customer' AS customer_type
FROM customers

-- Highlight and Execute
--select the part of the query you want to run and execute it to see the results.

              